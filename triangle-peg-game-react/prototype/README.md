# Prototype

Static HTML/CSS mock used to tune the triangle board sizing, spacing, and clip-path before wiring up React.

- Open `triange_peg_game_ui_prototype.html` in a browser to view the layout reference.
