import { Routes } from '@angular/router';
import { EqualizerComponent } from './equalizer/equalizer.component';
import { AppComponent } from './app';

export const routes: Routes = [
    { path: '', component: EqualizerComponent }
];
