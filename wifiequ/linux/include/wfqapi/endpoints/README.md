# WiFiEqu (Linux) — include/wfqapi/endpoints/

Header declarations for HTTP handlers:

- `channels.h` – `GET /api/v1/channels`.
- `stats.h` – `GET /api/v1/stats`.

Back to [`../README.md`](../README.md).
