# WiFiEqu (Linux) — src/wfq/mock/

Mock data source used when `mock=1` or the daemon runs without hardware access.

- `mocksignal.c` – sine-wave generator with configurable `refresh_wait_ms`.

Back to [`../README.md`](../README.md).
