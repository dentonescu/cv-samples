# WiFiEqu (Linux) — src/wfq/config/

Configuration helpers:

- `config.c` – parses `etc/wifiequd.conf`, caches contexts, exposes frequency-to-channel lookups.

Back to [`../README.md`](../README.md).
