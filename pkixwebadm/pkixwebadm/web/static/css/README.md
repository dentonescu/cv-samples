# CSS

Styling for pkixwebadm templates. `app.css` provides the custom palette/layout layers on top of the Bootstrap 5 CSS served via CDN (see `templates/base.html`). Utility classes are already available for the landing page widgets and will be expanded as the design slice grows.

Return to the [static README](../README.md) or the broader [web overview](../../README.md).
