# CSS

Styling for pkixwebadm templates. `app.css` currently provides the minimal layout used by the landing page. Bootstrap utility classes will return once the design slice expands.

Return to the [static README](../README.md) or the broader [web overview](../../README.md).
