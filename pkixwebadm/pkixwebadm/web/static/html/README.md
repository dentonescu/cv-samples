# HTML fallbacks

Static HTML used when the Jinja templating engine is unavailable. The FastAPI view swaps placeholders (`{PROJECT_NAME}`, `{VERSION}`, `{ERROR_MESSAGE}`) with runtime metadata before returning the response.

Return to the [static README](../README.md) or the wider [web overview](../../README.md).
